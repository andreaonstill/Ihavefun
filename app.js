'use strict';

// GSAP animation initialization
import { gsap } from 'gsap';

// Initialize icons from Lucide
import { iconName } from 'lucide'; // replace 'iconName' with actual icon names needed

// Function to initialize scroll animations
function initializeScrollAnimations() {
    // Add scroll animation logic here using GSAP
}

// Function to initialize interactive elements
function initializeInteractiveElements() {
    // Add interactive element logic here using GSAP
}

// Initialize the app
function init() {
    initializeScrollAnimations();
    initializeInteractiveElements();
}

window.addEventListener('load', init);